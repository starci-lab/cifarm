export * from "./query-static"

import { query1  as queryInventory1, queryMap as queryInventoryMap, QueryInventory, queryInventory } from "./query-inventory"
export {
    queryInventory1,
    queryInventoryMap,
    QueryInventory,
    queryInventory,
}

import {
    query1 as queryUser1,
    queryMap as queryUserMap,
    QueryUser,
    queryUser,
} from "./query-user"

export {
    queryUser1,
    queryUserMap,
    QueryUser,
    queryUser,
}