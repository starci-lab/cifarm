// import { createSlice, PayloadAction } from "@reduxjs/toolkit"
// import { ExtendedNFTData } from "./modal"

// export interface WrapSlice {
//   wrapNFTData?: ExtendedNFTData;
// }

// const initialState: WrapSlice = {
// }

// export const wrapSlice = createSlice({
//     name: "wrap",
//     initialState,
//     reducers: {
//         setWrapNFTData: (state, action: PayloadAction<ExtendedNFTData>) => {
//             state.wrapNFTData = action.payload
//         },
//     },
// })

// export const wrapReducer = wrapSlice.reducer
// export const { setWrapNFTData } = wrapSlice.actions
